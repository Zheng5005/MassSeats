// Payment endpoints will be registered here using Minimal API.
// Reference pattern (BookingService):
//
// var group = app.MapGroup("/api/payments");
// group.MapPost("/", ...);
// group.MapGet("/{id}", ...);
// group.MapGet("/booking/{bookingId}", ...);
// group.MapPost("/webhook", ...);
